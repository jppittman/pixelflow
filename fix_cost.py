# The methods `with_fma`, `with_fast_rsqrt`, and `fully_optimized` on `CostModel` are actually from `pixelflow-compiler/src/cost_builder.rs` where I saw them before!
# Oh, wait... I saw `build_cost_model_with_hce` and `parse_cost_model_toml` in `cost_builder.rs`.
# Maybe they were added as extension traits?
# Let's search for `fully_optimized`.
